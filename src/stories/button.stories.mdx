import { Meta, Story, Preview, Code, Props } from '@storybook/addon-docs/blocks';
import { Submit, Default, Disabled } from './button.stories';
import Button from '../components/atoms/button';

<Meta title="Atoms|Button" component={Button} />

# Button

This section describe all variant of Button component. You can play with 3 types: `submit`, `default` and `disabled`.

<div style={{ display: 'flex', justifyContent: 'space-between', width: '600px',backgroundColor: '#140C0B', padding: '20px' }}>
    <Submit onSubmit={e => alert('Envoyé')} />
    <Default onChange={e => alert('Clicked')} />
    <Disabled />
</div>

Now let's see in details !

```javascript
<Button type="submit" color='white' testid="submit-button" onSubmit={e => alert('Envoyé')}>Envoyer</Button>
<Button type="default" color='red' testid="default-button" onChange={e => alert('Envoyé')}>Envoyer</Button>

```

# Props

<Props of={Button} />