import { Meta, Story, Preview, Code, Props } from '@storybook/addon-docs/blocks';
import { Default, Custom} from './pageNumber.stories';
import PageNumber from '../components/atoms/pageNumber';

<Meta title="Atoms|PageNumber" component={PageNumber} />

# Page Number

This section describe all variant of PageNumber component. You can play with 2 types: `currentPage` and `totalPage`.

<div style={{ position: 'relative', height: '300px', background: 'black', padding: '20px 20px'  }}>
    <Custom />
</div>

Now let's see in details !

```javascript
<PageNumber />
<PageNumber currentPage="6" totalPage="8" />
```

# Props

<Props of={PageNumber} />