import { Meta, Story, Preview, Code, Props } from '@storybook/addon-docs/blocks';
import { Default, Custom } from './digits.stories';
import Digits from '../components/atoms/digits';

<Meta title="Atoms|Digits" component={Digits} />

# Digits

This section describe all variant of Digits component. You can play with 2 types: `digit` and `name`.

<div style={{ display: 'flex', flexDirection: 'column', justifyContent: 'center', background: 'black', padding: '20px 20px'  }}>
    <Default />
    <Custom />
</div>

Now let's see in details !

```javascript
<Digits />
<Digits digit="2,400,000,000" name="d'euros" />
```

# Props

<Props of={Digits} />