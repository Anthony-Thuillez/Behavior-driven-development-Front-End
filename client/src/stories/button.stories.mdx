import { Meta, Story, Preview, Code, Props } from '@storybook/addon-docs/blocks';
import { Default, Small, Medium, Large, White, Red, Disabled, Function } from './button.stories';
import Button from '../components/atoms/button';

<Meta title="Atoms|Button" component={Button} />

# Button

Example :

<div style={{ display: 'flex', flexDirection: 'column', justifyContent: 'space-between', alignItems: 'center', height: '550px', backgroundColor: '#140C0B', padding: '20px' }}>
    <Default />
    <Small />
    <Medium />
    <Large />
    <White />
    <Red />
    <Disabled />
    <Function />
</div>

How to use :

```javascript
<Button text="Default button" />;
<Button text="Small button" size="small" />;
<Button text="Medium button" size="medium" />;
<Button text="Large button" size="large" />;
<Button text="White button" color="white" />;
<Button text="Red button" color="red" />;
<Button text="Disabled button" disabled />;
<Button text="onClick button" onClick={() => alert('click !')} />;
```

# Props

<Props of={Button} />